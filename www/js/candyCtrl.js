/**
 * The main controller for the app. The controller:
 * - bind to the model via the angularfire service
 * - exposes the model to the template and provides event handlers
 */

var candyCtrl = angular.module('candy.controllers', []);
